const Ban = require('./ban');
const Grant = require('./grant');
const Joindate = require('./joindate');
const Prefix = require('./prefix');
const Role = require('./role');

module.exports = {
  Ban,
  Grant,
  Joindate,
  Prefix,
  Role,
};
